select employee_id,trim(employee_name) employee_name,lower(trim(email)) email,department_id,trim(job_title) job_title,join_date,exit_date,upper(trim(employment_status)) employment_status,salary,updated_at from {{ source('employee_raw','EMPLOYEES') }}