name: employee_analytics
version: "1.0.0"
config-version: 2
profile: employee_analytics
model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
models:
  employee_analytics:
    staging:
      +schema: STAGING
      +materialized: view
    marts:
      +schema: ANALYTICS
      +materialized: table
